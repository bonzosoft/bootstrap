volumes:
  ${VOL-NAME}:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /path/to/files


#docker volume create \
#  --driver local \
#  --opt type=none \
#  --opt o=bind \
#  --opt device=/path/to/files \
#  ${VOL-NAME}