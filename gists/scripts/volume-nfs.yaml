volumes:
  ${VOL-NAME}:
    driver: local
    driver_opts:
      type: nfs
      o: addr=${NFS-HOST},rw,nfsvers=4
      device: :/mnt/tank0/path/to/files


#docker volume create \
#  --driver local \
#  --opt type=nfs \
#  --opt o=addr=${NFS-HOST},rw,nfsvers=4 \
#  --opt device=:/mnt/tank0/path/to/files \
#  ${VOL-NAME}